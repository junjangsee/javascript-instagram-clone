{
  "name": "instagram-clone",
  "version": "0.0.1",
  "description": "instagram clone project",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node index.js",
    "build": "cp -f public/style.css build/style.css && cp -f public/index.html build/index.html && browserify index.js -o build/bundle.js"
  },
  "author": "junjang",
  "license": "ISC",
  "dependencies": {
    "browserify": "^16.5.2"
  }
}
